package com.webflux.study.testexample;

/**
 * reactor-test 모듈의 기능 사용을 위해 build.gradle의 dependencies 블록에 다음 의존성을 추가 해야함.
 * {@code
 *     dependencies {
 *         testImplementation 'io.projectreactor:reactor-test'
 *     }
 * }
 */
public class AboutTest {
}
